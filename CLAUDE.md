# Épure — repository guide for Claude

Épure (package/CLI: `epure`) is a grid-snapped, orthogonal-routed architecture-diagram editor that
reads/writes a tiny D2 subset as a git-reviewable file pair (`<name>.epr.d2` + `<name>.epr.layout.json`).
